 
function invertedRanges(ranges) {
  if(ranges.length === 0) return [[0, 100]]
  if(ranges[0].join("") === "0100") return []
  for(let i = 0; i <= ranges.length; i++){
    const previousHighestBound = ranges[i];
    
    for(let j )
  }
}