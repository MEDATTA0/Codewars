 
function maxConsecZeros(n){
  const toReadable = {↔}
  const zeros = n.toString(2).match(/(0)\1*/g)
  console.log(zeros)
  let max = 0;
  
    if(zero.length < max){
      return false
    }
    max = zero.length
    max
  })
  return max;
}